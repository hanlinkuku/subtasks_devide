# 当前版本留档：多视角候选机制改造之前

用户要求先 Git 留档。本提交保存现有实现，不实施新的算法改造。

## 当前可运行功能

- 使用 Qwen3-VL-32B 的自动划分流程；两条现有轨迹分别输出 11、7 个子任务。
- 本地工作台：`python workbench.py`，访问 `http://127.0.0.1:8765`。
- 四视角逐帧查看、动作时间线、边界编辑、拆分合并、撤销重做、修订保存与 JSON 导出。
- `export_annotation_replay.py` 可离线回放缓存结果，导出实际构造的图片、请求和完整 context。

## 已知限制，也是后续改造的原因

- 候选主要由左臂末端相对初始位置的距离峰值和回撤规则产生，带有墙面温控面板操作假设。
- 视觉交互核实使用一张头部画面和左腕序列，边界细化使用左腕序列，没有全程多视角动作发现兜底。
- 帧级输出不等于物理接触时刻已被精确验证；模型的不确定范围不是保证误差界。
- `outputs/ground_truth/v2` 为用户选定的工作参考，不是所有边界都经过精确人工确认的 ground truth。
- `outputs/confirmed` 等历史名称和记录保留用于追溯，是否确认应结合其中的撤回说明和当前参考元数据理解。
- 旧实验推理记录仅供追溯，不代表当前模型配置；当前主流程固定为 32B。

## 本次留档范围

保留代码、界面、需求、轻量标注及质量记录、模型推理 context 与解析结果、运动数据，以及回放的完整 context 和清单。

原始视频、解码图片、运行环境、回放中包含 Base64 图片的重复请求体及 ZIP 压缩包不纳入 Git，继续保留在本地。Git 检出本身不包含完整数据，重新运行需原始数据或已有帧文件与依赖环境。

最近一次完整回放：`outputs/replays/episode_000000_20260906_141651`，12 次逻辑 VL 调用、167 张输入图片、11 个子任务；使用缓存回答，实际网络调用为 0。

## 此版本已有验证

- 两条轨迹完整自动流程的缓存运行成功。
- 工作台后端 8 项检查通过；编辑器拆分/合并往返及连续边界检查通过。
- 页面检查覆盖轨迹切换、边界修改、拆分合并、撤销、后台运行及结果加载。
- 回放请求中的图片字节与单独导出的图片一致，最终标注与当前自动结果一致。

下一步讨论方向是全程多视角发现动作、多来源候选与局部边界细化；本提交尚未实现该方向。
