# 实验 004：场景预设消融的最终结果验收

预先登记。用户已说明不优先处理人工也难区分的微小帧差；不把现有 1—3 帧参考差异变成阈值调参目标，也不擅自定义所有场景通用的允许误差。

实验 003 只验收了中间视觉建议。本轮保持同一变量，补齐准备数据、候选、视觉核实、持续撤离细化、起点细化和最终导出的完整链路。独立目录执行，不覆写原正式结果或冻结基线。

使用实验 003 的真实模型回答和原流程的相同输入缓存。禁止新增网络调用，若缓存缺失立即失败；这验证已取得回答如何影响最终流程，不测量新的模型随机性或线上性能。

验收：两条轨迹最终技能/手臂序列、全部边界与基线对照；文本变化单独逐条核查，不用帧指标掩盖语义变化。若最终结构和边界未退化，才考虑去掉首次视觉提示中的具体物体、墙面和操作尖端断言。后续起点细化仍带场景假设，不能宣称全面去场景化。

## 结果与采用范围

独立目录完整运行通过，新增网络调用 0，缓存流程耗时约 6.7 秒（不含输入复制，不代表在线推理耗时）。报告：`outputs/experiments/004_scene_prior_e2e/report.json`。

两条最终结果分别 11、7 个子任务；技能、手臂和全部边界与基线一致，变动帧 0。首次视觉建议中的结束边界差异被原有持续撤离规则修正，起点细化仍使用同一候选和缓存。这说明实验 003 的中间结果变化不能直接当成最终退化；其原始结论保留为历史阶段判断，由本轮补充。

四处文字变化：按钮→电源按钮、升温按钮并保持→升温按钮、电源按钮保持语序变化、降温按钮→向下箭头按钮。与当前已认可的动作序列及对象功能一致；保持时段仍完整保留在 press 区间内。备注和目标属性未变。不将措辞变化计为边界或技能准确率提升。

采用范围：正式主流程的首次视觉核实默认不提供具体面板、墙面、未抓持和指定操作尖端断言。保留原提示作为显式对照参数；不增加额外复查或模型调用。候选生成、左腕输入、按钮类别问题、屏幕锚点、后续起点提示及输出目标仍带场景限制。本轮只证明移除此处先验未使当前最终标注退化，不证明未来数据泛化。
