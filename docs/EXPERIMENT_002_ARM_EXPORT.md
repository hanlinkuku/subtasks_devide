# 实验 002：候选手臂不应在导出时丢失

预先登记：右臂候选进入原导出器后，活动片段全部写成 left。只修复导出手臂及配套固定文字，不改变候选、模型提示或边界。

验收：右臂候选输出 right，静止输出 none；单臂候选与视觉结果手臂冲突时保留旧文件并报错；两条现有正式标注与冻结基线完整一致。混合双臂时序尚未实现，不强行合并到单臂输出。此实验不证明真实右臂视觉识别准确。

## 结果

已用修改前提交 `0d60f27` 的导出代码及修改后代码执行同一个构造反例：输入 right，旧输出 left，新输出 right，边界不变。完整前后 JSON 位于 `outputs/experiments/002_arm_export/counterexample.json`，不是模拟真实右臂视频。

两条完整正式流程缓存复跑成功，比较报告显示标注完整内容与基线一致、变动帧数均为 0。53 项检查通过。没有新增模型请求，没有改提示词、候选或边界规则。

采用导出修复。它纠正了可复现的信息丢失，但完整流程的视觉处理仍预设左臂；任务名和目标类型也仍有温控面板假设，不能据此宣称场景泛化已经完成。
